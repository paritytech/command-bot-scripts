{
  "$schema": "../../schema.cmd.json",
  "command": {
    "name": "try-runtime",
    "configuration": {
      "gitlab": {
        "job": {
          "tags": ["linux-docker"]
        },
        "variables": {}
      },
      "commandStart": [ "\"$PIPELINE_SCRIPTS_DIR/commands/try-runtime/try-runtime.sh\"" ]
    },
    "presets": [
      {
        "name": "try-runtime",
        "args": [
          { "name": "Chain", "rule": "/^[a-z_-]+$/" },
          { "name": "URI", "one_of": [".*"] }
        ]
      }
    ]
  }
}
