{
  "$schema": "../../node_modules/command-bot/src/schema/schema.cmd.json",
  "command": {
    "description": "This is a wrapper to run `bench` for all runtimes for specific pallet. In order to skip merging master - add `-v UPSTREAM_MERGE=n` to env variables",
    "configuration": {
      "gitlab": {
        "job": {
          "tags": [
            "weights-vm"
          ],
          "variables": {}
        }
      }
    },
    "presets": {
      "default": {
        "description": "Pallet Benchmark for Substrate/Polkadot/Cumulus for specific pallet",
        "repos": ["polkadot-sdk"],
        "args": {
          "pallet": { "label": "Pallet", "type_rule": "^([a-z_]+)([:]{2}[a-z_]+)?$", "example": "pallet_name" }
        }
      }
    }
  }
}
